let myBook = {
    title: '1984',
    author: 'George Orwell',
    pageCount: 326
}
console.log(`${myBook.title} by ${myBook.author}`)

 myBook.title = 'Animal Farm'

 console.log(`${myBook.title} by ${myBook.author}`)

 //name, age ,location

 //marwen is and lives in tunis.

 let myObject = {
     name: 'Marwen',
     age: 26,
     location: 'Tunis'
 }
 console.log(`${myObject.name} is ${myObject.age } lives in ${myObject.location}`)
myObject.age = myObject.age + 1 
console.log(`${myObject.name} is ${myObject.age } lives in ${myObject.location}`)
